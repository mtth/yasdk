import {createSdk} from './yasdk.gen';

export {createSdk} from './yasdk.gen';


const sdk = createSdk('');

sdk['actions/add-custom-labels-to-self-hosted-runner-for-repo']({
  body: {
    labels: []
  },
});
